services:
  - type: web
    name: armes-cendres-api
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn src.armes_cendres.api:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PYTHON_VERSION
        value: 3.12.0
